window.APP_CONFIG = {
  // For local Flask + frontend served by Flask, keep empty string.
  // For Netlify frontend + remote backend, set your backend origin:
  // API_BASE_URL: "https://your-backend.onrender.com"
  API_BASE_URL: "",
};
